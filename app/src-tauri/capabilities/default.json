{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window. Girsa never needs the network (spec.md §14) — no shell, no filesystem plugin. Everything it reads goes through the commands in lib.rs. The one dialog permission is *choose the directory your semantic-lane model is in* (W30); it opens a picker and grants no read of anything.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
