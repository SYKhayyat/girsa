{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window. Girsa is offline (spec.md §14) — no network, no shell, no filesystem plugin. Everything it reads goes through the commands in lib.rs.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
